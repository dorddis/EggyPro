export const metadata = {
  title: 'Secure Checkout | EggyPro',
  description: 'Complete your EggyPro order securely.',
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 